# Phase 14 - Final Package Checklist

Status: blocked pending manual QA evidence.

## Package Contents

| Item | Status | Notes |
| --- | --- | --- |
| Root React launcher | Ready | `index.html` launches `./react-app/index.html`. |
| React production bundle | Ready | `react-app/` exists and is generated by `npm run build`. |
| PWA manifest | Ready | `start_url` points to `./react-app/index.html?v=1.0`. |
| Service worker | Ready | Cache version is `blue-wallet-react-v1.0.0`. |
| Legacy rollback file | Ready | `legacy-root-pwa.html` is preserved. |
| Draft release notes | Ready as draft | `docs/RELEASE-NOTES-v1.0-DRAFT.md`. |
| Final release notes | Blocked | Do not create `docs/RELEASE-NOTES-v1.0.md` until manual QA sign-off is complete. |
| Manual QA checklist | Blocked | Evidence fields remain blank. |
| Release tag | Blocked | Do not create `v1.0.0` yet. |

## Phase 14 Command Results

| Command | Result | Notes |
| --- | --- | --- |
| `npm test` | Passed | 11 files, 58 tests. |
| `npm run build` | Passed | `react-app/` bundle generated successfully. |
| `npm run lint` | Passed with warnings | Existing fast-refresh warnings only. |

## Final Packaging Commands

Commands required for every final packaging attempt:

```bash
cd next-app
npm test
npm run build
npm run lint
```

## Protected File Checks

Before tagging, verify:

- `legacy-root-pwa.html` exists and is unchanged unless an explicit rollback documentation update is approved.
- Root `index.html` still launches `./react-app/index.html`.
- `manifest.json` still starts at `./react-app/index.html?v=1.0`.
- `service-worker.js` still uses `blue-wallet-react-v1.0.0`.
- No code writes to `SeafarerWalletDB`.

## Tagging Preconditions

`v1.0.0` can be created only when:

- Automated checks pass.
- Manual QA evidence is complete for every required browser/device or formally waived.
- Install/offline evidence is recorded.
- Vault, backup/restore, wrong PIN, key rotation, OCR, PDF fallback, sea-service persistence, and rollback checks are recorded.
- Engineering, QA, and product owner sign-off is complete.
- The release branch is clean.

## Current Packaging Outcome

The package is technically buildable, but not release-approved. The final release remains blocked on manual evidence.
